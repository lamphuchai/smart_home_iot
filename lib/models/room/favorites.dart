import 'package:smart_home_iot/models/room/device.dart';

class Favorites {
  final int indexRoom;
  final int indexDevice;
  final Device device;
  Favorites({required this.indexRoom, required this.indexDevice,required this.device});
  
}
